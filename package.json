{
  "name": "node-starter",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "frontend": "http-server",
    "backend": "json-server -w db.json"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "http-server": "^0.12.3",
    "mysql2": "^3.5.2"
  },
  "devDependencies": {
    "json-server": "^0.16.3"
  }
}
